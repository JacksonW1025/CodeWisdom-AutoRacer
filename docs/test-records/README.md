# Test Records

This directory stores committed summaries for phase acceptance evidence. Raw logs,
rosbags, maps, and large serial captures stay outside normal git unless a record
explicitly says they are small and necessary for reproducing the conclusion.

Use the templates in each stage directory and keep case names aligned with the
root workspace acceptance contract.
